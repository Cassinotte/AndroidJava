<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#8B8B83" android:padding="16dp">

    <TextView
        android:id="@+id/labelUsuario"
        android:layout_width="55dip"
        android:layout_height="wrap_content"
        android:text="@string/usuario" />

    <!-- Ao lado do label Usúario "Layout_toRight" -->
    <EditText
        android:id="@+id/campoUsuario"
        android:gravity="left"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:drawable/editbox_background"
        android:layout_toRightOf="@+id/labelUsuario"
        android:inputType="text" />

    <!-- Senha: abaixo do campoUsuario "layout_bellow", alinhado a esquerda -->
    <TextView
        android:layout_width="55dip"
        android:layout_height="wrap_content"
        android:layout_below="@id/campoUsuario"
        android:id="@+id/labelSenha"
        android:text="@string/senha" />

    <!-- Ao loado do label Senha "layout_toRight" -->
    <EditText
        android:id="@+id/campoSenha"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/labelSenha"
        android:layout_alignTop="@id/labelSenha"
        android:inputType="textPassword" />

    <!-- Abaxio do campoSenha "layout_below", alinha a direita -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="35dip"
        android:id="@+id/btLogin"
        android:layout_marginTop="10dp"
        android:layout_below="@id/campoSenha"
        android:layout_alignParentRight="true"
        android:text="@string/login"




</RelativeLayout>